# A sample Guardfile
# More info at https://github.com/guard/guard#readme

guard 'sass', :input => 'stylesheets/sass', :output => 'stylesheets/css'

guard 'livereload' do
  watch(%r{.+\.(css|js|html)$})
end
